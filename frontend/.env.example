# Keep this unset locally so Vite proxies /api to localhost:8000.
# Set it in Railway to the backend's public API URL before building the frontend.
VITE_API_BASE_URL=https://your-backend-domain.up.railway.app/api
